---
phase: 08-meta-funding-assistant-safe-pilot
plan: 04
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - apps/browser-extension/src/detector/types.ts
  - apps/browser-extension/src/detector/url.ts
  - apps/browser-extension/src/detector/dom.ts
  - apps/browser-extension/src/detector/evaluate.ts
  - apps/browser-extension/src/content.tsx
  - apps/browser-extension/src/overlay/Overlay.tsx
  - apps/browser-extension/src/overlay/styles.css
  - apps/browser-extension/test/fixtures/no-payment.html
  - apps/browser-extension/test/fixtures/qr-active.html
  - apps/browser-extension/test/fixtures/account-mismatch.html
  - apps/browser-extension/test/detector.test.ts
  - apps/browser-extension/manifest.json
  - apps/browser-extension/vite.config.ts
autonomous: true
requirements: [REQ-TOPUP-01, REQ-TOPUP-02]
user_setup: []
must_haves:
  truths:
    - "The extension reads only top-frame approved Meta Billing pages and never alters Meta controls or payment behavior."
    - "URL account evidence is independently cross-checked with visible dialog/page evidence and mismatch blocks mapping."
    - "Amount, INR, QR presence, account evidence, and detector version are normalized without capturing QR payload or full HTML."
  artifacts:
    - "Pure detector modules are covered by sanitized fixture tests."
    - "A Shadow DOM overlay presents detected context and a blocked mismatch state."
  key_links:
    - "Content script sends only validated normalized evidence to the service worker."
---

<objective>
Build the read-only Meta Billing detector and isolated mapping overlay against sanitized fixtures.

Purpose: Prove REQ-TOPUP-01 and REQ-TOPUP-02 without relying on brittle production selectors or modifying Meta behavior.
Output: Pure detector pipeline, bounded SPA observer, Shadow DOM overlay, and fixture tests.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-meta-funding-assistant-safe-pilot/08-02-SUMMARY.md
@.planning/phases/08-meta-funding-assistant-safe-pilot/08-CONTEXT.md
@.planning/phases/08-meta-funding-assistant-safe-pilot/08-RESEARCH.md
@apps/browser-extension/manifest.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement URL-first multi-signal detection</name>
  <files>apps/browser-extension/src/detector/types.ts, apps/browser-extension/src/detector/url.ts, apps/browser-extension/src/detector/dom.ts, apps/browser-extension/src/detector/evaluate.ts, apps/browser-extension/test/fixtures/no-payment.html, apps/browser-extension/test/fixtures/qr-active.html, apps/browser-extension/test/fixtures/account-mismatch.html, apps/browser-extension/test/detector.test.ts</files>
  <behavior>
    - URL parser extracts normalized candidates such as act without assuming URL evidence is final truth.
    - DOM parser returns visible account ID/name, INR amount, QR-present boolean, dialog state, success state, and sanitized signal names only.
    - Equal URL/visible IDs yield high confidence; mismatched IDs yield blocked; missing visible ID cannot auto-confirm.
    - Fixture parsing never returns QR content, image data, page HTML, Meta access tokens, or arbitrary text dumps.
  </behavior>
  <action>Create pure parsers using native URL and DOM APIs plus shared schemas. Use a declarative detector-rule object with version and kill-switch fields, but keep fixture selectors generic until live calibration. Parse Indian currency deterministically and treat QR only as presence of an allowed marker. Hash normalized snapshots to suppress duplicate observer output. Write node:test fixture cases first; use a minimal local DOM fixture adapter already justified by the extension build, without adding a framework unless execution proves native facilities insufficient.</action>
  <verify><automated>pnpm --filter @ads-control/browser-extension run test</automated></verify>
  <done>Fixtures prove detection, non-detection, and blocking mismatch while the evidence object contains only the approved normalized fields.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the read-only Shadow DOM assistant</name>
  <files>apps/browser-extension/src/content.tsx, apps/browser-extension/src/overlay/Overlay.tsx, apps/browser-extension/src/overlay/styles.css, apps/browser-extension/manifest.json, apps/browser-extension/vite.config.ts</files>
  <action>Run only in the top frame on explicit facebook.com and business.facebook.com billing/payment routes. Observe history changes and a scoped dialog root with a debounced, bounded MutationObserver; disconnect on route exit. Mount one ShadowRoot so Meta CSS cannot affect the overlay. Show detected account, amount, currency, confidence, and clear mismatch/missing-context states; do not click, disable, replace, cover, or rewrite Meta controls. Send only validated evidence messages to the service worker. Keep controls keyboard accessible and use the project's clean sentence-case visual language.</action>
  <verify><automated>pnpm --filter @ads-control/browser-extension run test &amp;&amp; pnpm --filter @ads-control/browser-extension run build</automated></verify>
  <done>The packaged content script renders one isolated assistant only on approved routes and remains read-only under repeated SPA navigation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Meta DOM to content script | Meta-controlled and potentially hostile DOM becomes normalized evidence. |
| Content script to service worker | Messages cross an extension privilege boundary. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-11 | Tampering | DOM evidence | high | mitigate | Independent URL/visible evidence and schema validation. |
| T-08-12 | Information disclosure | Detector payload | critical | mitigate | Field allow-list and tests excluding QR payload, HTML, tokens, and screenshots. |
| T-08-13 | Denial of service | MutationObserver | medium | mitigate | Scoped observation, debounce, snapshot hashing, and route disconnect. |
</threat_model>

<verification>
- Detector fixture suite passes.
- Extension build succeeds with approved hosts only.
- Static inspection finds no DOM mutation or Meta control click calls.
</verification>

<success_criteria>
- REQ-TOPUP-01 and REQ-TOPUP-02 are proven against sanitized fixtures.
- Production selectors remain gated on live calibration rather than inferred from screenshots.
</success_criteria>

<output>
Create `.planning/phases/08-meta-funding-assistant-safe-pilot/08-04-SUMMARY.md` when done.
</output>
