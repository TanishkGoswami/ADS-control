---
phase: 08-meta-funding-assistant-safe-pilot
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - packages/database/prisma/schema.prisma
  - packages/database/prisma/migrations/20260918_phase8_topup_domain/migration.sql
  - packages/shared/src/enums/index.ts
  - packages/shared/src/types/index.ts
  - packages/shared/src/schemas/index.ts
  - packages/shared/src/topup/domain.ts
  - packages/shared/test/topup-domain.test.ts
autonomous: true
requirements: [REQ-TOPUP-03, REQ-TOPUP-04, REQ-TOPUP-05]
user_setup: []
must_haves:
  truths:
    - "Funding requests and direct lots share one validated mapping contract using INR minor-unit strings."
    - "Detected and selected accounts remain separate, events are append-only, and operational and financial states cannot be conflated."
    - "Reservations can prevent over-commitment without changing FundLot balances or creating ledger entries."
  artifacts:
    - "Prisma schema contains FundingRequest, MetaTopupSession, MetaTopupEvent, and TopupReservation."
    - "Shared contracts centralize states, parsing, confidence, transitions, and idempotency fingerprints."
  key_links:
    - "Every request/session/reservation/event carries organizationId and uses compound uniqueness for tenant-safe idempotency."
---

<objective>
Define and test the funding-capture domain before controllers, UI, or browser observation expand it.

Purpose: Make money, state, reservation, and idempotency invariants explicit and reusable across API, web, and extension.
Output: Prisma domain migration plus shared typed contracts and pure domain tests.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-meta-funding-assistant-safe-pilot/08-01-SUMMARY.md
@.planning/phases/08-meta-funding-assistant-safe-pilot/08-CONTEXT.md
@.planning/phases/08-meta-funding-assistant-safe-pilot/08-RESEARCH.md
@packages/database/prisma/schema.prisma
@packages/shared/src/enums/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify top-up states and deterministic validation</name>
  <files>packages/shared/src/enums/index.ts, packages/shared/src/types/index.ts, packages/shared/src/schemas/index.ts, packages/shared/src/topup/domain.ts, packages/shared/test/topup-domain.test.ts</files>
  <behavior>
    - INR amount text converts to integer paise with no floating-point arithmetic; non-INR is rejected.
    - Account IDs normalize to digits and URL candidates remain separate from visible-context candidates.
    - URL and visible ID equality yields high confidence; mismatch yields blocked; name-only evidence requires confirmation.
    - Operational transitions end at DETECTED, MAPPED, UI_OBSERVED, REVIEW_REQUIRED, CANCELLED, or EXPIRED while financial review remains UNVERIFIED, CONFIRMED, or REJECTED.
    - Canonical request fingerprints are stable across key order and differ when semantic payload changes.
  </behavior>
  <action>Write failing node:test cases first, then add centralized enums, API wire types, strict Zod schemas, pure INR parser, digit-only account normalizer, evidence evaluator, state-transition table, and SHA-256-ready canonical idempotency fingerprint input. Use decimal minor-unit strings at all boundaries and BigInt only after validation. Unknown DOM-derived fields must be stripped or rejected. Cover REQ-TOPUP-03 through REQ-TOPUP-05 without duplicating literals in later packages.</action>
  <verify><automated>pnpm --filter @ads-control/shared exec node --import tsx --test test/topup-domain.test.ts &amp;&amp; pnpm --filter @ads-control/shared run build</automated></verify>
  <done>Shared tests prove parsing, evidence mismatch, transition, and fingerprint behavior with exact expected outputs.</done>
</task>

<task type="auto">
  <name>Task 2: Add tenant-scoped funding and top-up persistence</name>
  <files>packages/database/prisma/schema.prisma, packages/database/prisma/migrations/20260918_phase8_topup_domain/migration.sql</files>
  <action>Add FundingRequest, MetaTopupSession, MetaTopupEvent, and TopupReservation with explicit organization, actor/device, FundLot, AdAccount, and request relations. FundingRequest stores reference, amountMinor BigInt, INR currency, purpose, optional target account, and lifecycle. Session stores idempotency key/fingerprint, detected and selected account IDs separately, detected/selected amount and currency, funding source choice, confidence, operational state, financial review state, detector version, and timestamps. Event is append-only with per-session sequence and unique idempotency key. Reservation stores amount, status, expiry/release/confirmation timestamps and unique active ownership. Add indexes for organization/status/time and constraints preventing duplicate session/event keys. Do not add any relation to FinancialLedgerTransaction and do not mutate FundLot currentAmountMinor.</action>
  <verify><automated>pnpm --filter @ads-control/database run db:generate &amp;&amp; pnpm --filter @ads-control/database run build</automated></verify>
  <done>The generated Prisma client exposes all four models and the SQL migration can create and roll back the domain without ledger side effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Shared wire contract to domain | Untrusted page-derived strings become money and account evidence. |
| API transaction to database | Concurrent mappings compete for the same FundLot capacity. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-04 | Tampering | Money and account evidence | high | mitigate | Strict Zod contracts, BigInt minor units, and independent evidence fields. |
| T-08-05 | Repudiation | Event history | high | mitigate | Append-only sequenced events and immutable actor/device references. |
| T-08-06 | Denial of service | Idempotency keys | medium | mitigate | Bounded lengths, compound unique indexes, and deterministic fingerprints. |
</threat_model>

<verification>
- Shared top-up tests and builds pass.
- Prisma generate and database package build pass.
- Schema inspection confirms no top-up model can create a ledger posting.
</verification>

<success_criteria>
- REQ-TOPUP-03, REQ-TOPUP-04, and REQ-TOPUP-05 have complete persistence and contract foundations.
- State and money invariants are tested before API implementation.
</success_criteria>

<output>
Create `.planning/phases/08-meta-funding-assistant-safe-pilot/08-02-SUMMARY.md` when done.
</output>
