---
phase: 08-meta-funding-assistant-safe-pilot
plan: 06
type: execute
wave: 5
depends_on: ["08-03", "08-04", "08-05"]
files_modified:
  - apps/browser-extension/src/background.ts
  - apps/browser-extension/src/api.ts
  - apps/browser-extension/src/queue.ts
  - apps/browser-extension/src/content.tsx
  - apps/browser-extension/src/overlay/Overlay.tsx
  - apps/browser-extension/src/detector/rules.ts
  - apps/browser-extension/test/queue.test.ts
  - apps/browser-extension/test/integration.test.ts
  - apps/api/test/meta-funding.e2e.spec.ts
  - .planning/phases/08-meta-funding-assistant-safe-pilot/08-CALIBRATION.md
autonomous: false
requirements: [REQ-TOPUP-01, REQ-TOPUP-02, REQ-TOPUP-03, REQ-TOPUP-04, REQ-TOPUP-05, REQ-TOPUP-06]
user_setup:
  - "Chrome with developer mode enabled and access to the pilot Meta Business English Billing UI."
  - "One approved pilot Funding Request or eligible INR FundLot and 1-2 synchronized Ad Accounts."
  - "A disposable/test payment flow approved for calibration; do not perform an unapproved real transfer."
must_haves:
  truths:
    - "Pair, detect, map, observe, retry, verify, and review form one idempotent end-to-end workflow."
    - "Browser/service-worker restart, offline retry, duplicate success, multiple tabs, cancellation, and expiry create no duplicate sessions or ledger effects."
    - "Live selectors are accepted only after sanitized calibration against the real English Meta Billing UI."
  artifacts:
    - "Durable chrome.storage.local queue retries idempotent event envelopes."
    - "08-CALIBRATION.md records sanitized route, selector, wording, fixture, and acceptance evidence without customer data."
  key_links:
    - "Overlay mapping uses API eligibility and the detected/selected mismatch block before session confirmation."
    - "Success observation is queued to the service worker, acknowledged by the API once, then appears in web review."
---

<objective>
Complete the safe-pilot workflow, harden retries and edge cases, then calibrate the detector on the real Meta Billing DOM before acceptance.

Purpose: Turn the independently proven layers into one reliable pilot while preserving the no-payment, no-auto-ledger boundary.
Output: Durable integration, end-to-end tests, calibrated rules/fixtures, and signed-off pilot evidence.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
@.agents/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/08-meta-funding-assistant-safe-pilot/08-03-SUMMARY.md
@.planning/phases/08-meta-funding-assistant-safe-pilot/08-04-SUMMARY.md
@.planning/phases/08-meta-funding-assistant-safe-pilot/08-05-SUMMARY.md
@.planning/phases/08-meta-funding-assistant-safe-pilot/08-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire mapping, durable event retry, and end-to-end acceptance</name>
  <files>apps/browser-extension/src/background.ts, apps/browser-extension/src/api.ts, apps/browser-extension/src/queue.ts, apps/browser-extension/src/content.tsx, apps/browser-extension/src/overlay/Overlay.tsx, apps/browser-extension/test/queue.test.ts, apps/browser-extension/test/integration.test.ts, apps/api/test/meta-funding.e2e.spec.ts</files>
  <behavior>
    - High-confidence matching selects and locks the exact account; medium confidence asks confirmation; low confidence requires manual search; mismatch cannot confirm.
    - Queue survives service-worker restart, retries network/5xx with bounded exponential backoff and jitter, pauses on 401/revoke, and removes only acknowledged 2xx events.
    - Duplicate signals and multiple tabs resolve to one top-up session and ordered event sequence.
    - Closing, cancellation, and expiry release reservations; UI_OBSERVED remains review-only.
  </behavior>
  <action>Connect the overlay to authenticated assets, eligible requests/direct lots, detect/map/event APIs, and visible status. Store bounded envelopes in chrome.storage.local with local event ID, server idempotency key, session key, type, sanitized payload, createdAt, attempts, and nextAttemptAt. Drain on startup, alarms, online/message events, and heartbeat; use one in-flight drain lock and cap queue size/retention. Preserve page-session and tab identifiers without making them financial identity. Add extension integration and API e2e tests covering offline retry, restart, duplicate success, multiple tabs, mismatch, amount mismatch, cancellation, expiry, revoke, and explicit zero ledger writes. Do not add BullMQ, remote executable code, QR decoding, screenshots, network interception, or Meta control automation.</action>
  <verify><automated>pnpm --filter @ads-control/browser-extension run test &amp;&amp; pnpm --filter @ads-control/browser-extension run build &amp;&amp; pnpm --filter @ads-control/api exec node --import tsx --test test/meta-funding.e2e.spec.ts</automated></verify>
  <done>The fixture-driven end-to-end path produces exactly one session and ordered events under retries/restarts, and every failure case preserves reservation and ledger invariants.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Calibrate against the live Meta Billing DOM</name>
  <what-built>An unpacked MV3 pilot extension and ADS Control funding workspace with fixture-verified detection, mapping, retry, review, and revoke behavior.</what-built>
  <how-to-verify>On the approved pilot PC, open the English Meta Billing page for each of the 1-2 pilot accounts. Inspect URL changes and visible DOM for QR active, processing, success, failure, and expired states. Confirm the extension does not modify or cover Meta controls; URL and visible account agree before auto-selection; mismatch blocks; amount/INR/QR presence are correct; no QR payload, screenshot, full HTML, Meta token, or payment credential appears in DevTools, API payloads, logs, AuditLog, or events. Record only sanitized selector notes and synthetic fixture fragments in 08-CALIBRATION.md. Do not complete an unapproved payment.</how-to-verify>
  <resume-signal>Type "calibration approved" with sanitized observations, or describe each failed state.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply calibrated rules and run the pilot acceptance matrix</name>
  <files>apps/browser-extension/src/detector/rules.ts, apps/browser-extension/test/fixtures/qr-active.html, apps/browser-extension/test/fixtures/account-mismatch.html, apps/browser-extension/test/detector.test.ts, .planning/phases/08-meta-funding-assistant-safe-pilot/08-CALIBRATION.md</files>
  <action>Convert approved sanitized observations into versioned declarative rules and synthetic fixtures; do not copy customer names, IDs, QR material, or full Meta markup. Record tested route patterns, selector rationale, text variants, detector version, extension version, timestamp, operator/device, and pass/fail matrix. Re-run fixture regression and the approved pilot flow: pair, detect exact account/amount, map request or direct lot, observe success when safely available, targeted refresh, and review. Exercise mismatch, duplicate success, offline retry, restart, cancellation, and expiry. Keep the backend kill switch enabled by default until all mandatory states pass; enable only the one-operator pilot after approval.</action>
  <verify><automated>pnpm --filter @ads-control/browser-extension run test &amp;&amp; pnpm --filter @ads-control/browser-extension run build &amp;&amp; pnpm --filter @ads-control/api run build &amp;&amp; pnpm --filter @ads-control/web run build</automated></verify>
  <done>Calibration record is sanitized and complete, fixtures reproduce approved signals, all builds/tests pass, and the kill switch is enabled only for the defined one-operator safe pilot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Extension local storage to API | Durable untrusted envelopes may be replayed after restarts. |
| Live Meta UI to detector rules | Private DOM can drift and expose sensitive material. |
| Human calibration to repository | Observations must be sanitized before becoming fixtures/docs. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-17 | Tampering | Retry queue | high | mitigate | Schema validation, fingerprints, one drain lock, server idempotency. |
| T-08-18 | Information disclosure | Calibration artifacts | critical | mitigate | Synthetic fixtures and explicit sensitive-field inspection checkpoint. |
| T-08-19 | Denial of service | Retry behavior | medium | mitigate | Queue cap, retention, bounded backoff, pause on permanent auth failure. |
| T-08-20 | Spoofing | Multiple tabs/signals | high | mitigate | Page-session identity plus server uniqueness and exact account cross-check. |
</threat_model>

<verification>
- Extension, API, and web builds pass.
- Detector, queue, integration, and e2e tests pass.
- Human calibration checks every required Meta state and sensitive-data boundary.
- Database assertions show one session/event sequence and zero ledger transactions for the pilot observation.
</verification>

<success_criteria>
- REQ-TOPUP-01 through REQ-TOPUP-06 are satisfied end to end.
- One approved pilot top-up yields exactly one auditable session and review item.
- All specified mismatch, retry, restart, duplicate, cancellation, and expiry cases are safe.
- No BullMQ and no automatic ledger posting exist in the implementation.
</success_criteria>

<output>
Create `.planning/phases/08-meta-funding-assistant-safe-pilot/08-06-SUMMARY.md` when done.
</output>
