{
  "name": "@ads-control/browser-extension",
  "version": "1.0.9",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b && vite build",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "package": "pnpm run build && node scripts/package.mjs"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.8",
    "@types/chrome": "^0.0.287",
    "adm-zip": "^0.6.1",
    "typescript": "^5.8.2",
    "vite": "^6.2.0"
  }
}
